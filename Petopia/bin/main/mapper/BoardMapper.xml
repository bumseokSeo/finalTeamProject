<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.myapp.dao.BoardDAO">
	<insert id="shareInsert">
		insert into board(userid, title, content, filename1, boardtype)
		values(#{userid},#{title},#{content},#{filename1},#{boardtype})
	</insert>
	<select id="selectList" resultType="BoardVO">
		select boardno, title, userid, date_format(writedate,"%m/%d") writedate, filename1
		from board where boardtype=#{boardtype} order by boardno desc
	</select>
</mapper>